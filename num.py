import numpy as np
a = np.array([1, 2, 3])
# The `print(a)` statement is printing the contents of the numpy array `a` to the console. In this
# case, it will print `[1 2 3]`, which are the elements of the array `a`.
print(a)
"""main  benifits of numpy 
        less memory
        fast
        convinient
    """
